# nginx proxy address - http://172.17.0.1:3000

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "5m"
    max-file: "1"

services:
  standardnotes:
    image: standardnotes/standardnotes:latest
    container_name: standardnotes-app
    restart: unless-stopped
    logging: *default-logging
    networks:
      - standardnotes
    ports:
      - "3000:3000"
    environment:
      TZ: ${TZ:-America/New_York}
      PUID: ${SERVICE_USER:-1000}
      PGID: ${SERVICE_GROUP:-1000}
    volumes:
      - ./rootfs/config/standardnotes:/config
      - ./rootfs/data/standardnotes:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  standardnotes:
    external: false
